using {{namespace}}.EntityFrameworkCore;
using {{namespace}}.{{pascalCase pluralName}};
using System;
using Volo.Abp.EntityFrameworkCore;

namespace {{namespace}}.Repositories;

/// <inheritdoc />
public class {{pascalCase name}}Repository(IDbContextProvider<{{pascalCase project}}DbContext> dbContextProvider) : {{pascalCase project}}Repository<{{pascalCase name}}, {{pascalCase primaryKey}}>(dbContextProvider), I{{pascalCase name}}Repository
{
    
}

